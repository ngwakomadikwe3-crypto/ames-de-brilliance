"use client";
import {CustomerProvider,useCustomer} from '@/components/CustomerState';
function Content(){const c=useCustomer();const byId=new Map(c.catalog.assets.map(a=>[a.id,a]));return <main style={{minHeight:'70vh',padding:'32px 20px',maxWidth:720,margin:'0 auto',background:'#0b0d10',color:'#eef2f6'}}><h1>AMES Favorites</h1>{!c.ready?<p>Loading favorites…</p>:!c.user?<p>Sign in to view your saved pieces.</p>:<>{!c.state.favorites.length&&!c.state.saved.length?<p>No saved pieces yet.</p>:<ul>{[...c.state.favorites,...c.state.saved].map((item,i)=>{const a=byId.get(item.assetId);return <li key={`${item.assetId}-${i}`} style={{padding:'12px 0',borderBottom:'1px solid #303840'}}>{a?.name||item.assetId}</li>;})}</ul>}</>}</main>}
export default function FavoritesPage(){return <CustomerProvider><Content/></CustomerProvider>}
