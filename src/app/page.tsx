import AppPage from "./app/page";

export const dynamic = "force-dynamic";

/** The AMES product shell is the public homepage. Legacy dealer workflows remain on their dedicated routes. */
export default AppPage;
